<h1>Products#index</h1> 

<%= link_to "New Product", "/products/new" %>

<ul>
<% @products.each do |product| %>
  <li> <%= product[:description] %>, <%= product[:name] %>, <%= product[:price_in_cents] %> </li>
<% end %>
</ul>